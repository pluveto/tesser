[package]
name = "tesser-events"
version = "0.1.0"
edition = "2021"

[dependencies]
tesser-core = { path = "../tesser-core" }
tokio = { version = "1", features = ["sync"] }
serde = { version = "1", features = ["derive"] }
